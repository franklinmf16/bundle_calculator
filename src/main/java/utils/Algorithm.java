package utils;

import model.Input;
import model.config.Config;

import java.util.ArrayList;


public class Algorithm {
    private Input input;
    private Config config;

    public Algorithm(Input input, Config config) {
        this.input = input;
        this.config = config;
    }

    private ArrayList<Integer> getBundleAsList (Config config, String category){
        ArrayList<Integer> bundleList = new ArrayList<Integer>();

        return null;
    }






}
